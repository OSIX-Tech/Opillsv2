---
import { Image } from 'astro:assets';
import logo from '../assets/logo.png';

interface Props {
  class?: string;
}
const { class: className } = Astro.props;
---
<Image src={logo} alt="oPills Logo" class={className} /> 